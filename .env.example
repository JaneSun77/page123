# Copy this file to .env and fill in the values.
# .env is listed in .gitignore and must never be committed.

# (Optional) API endpoint for the contact form.
# If left empty, the form will simulate success without making a network request.
ENV_CONTACT_ENDPOINT=

# (Optional) Site title displayed in the browser tab and metadata.
ENV_SITE_NAME=page123
